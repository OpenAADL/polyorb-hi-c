"sunseeker" example:

* Description: this is an example provided by CMU SEI to test some 
  AADL features, and their code generation patterns

* The AADL model used is conform to AADLv2

* Execution: 
    - local: to run the local LEON version, simply simulate it by 
      issuing tsim-leon sunseeker' and then type 'go' in the Tsim prompt

    - distributed : to run the distributed version, run the 
      'sunseekercontroller' executable then the 'sunseekerplant' one 
      in two different terminals.
