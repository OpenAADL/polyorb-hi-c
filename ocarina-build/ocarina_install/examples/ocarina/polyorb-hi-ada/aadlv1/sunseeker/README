Description:

This is an example provided by CMU SEI to test some AADL features, and
their code generation patterns

Execution:

To run the local LEON version, simply simulate it by issuing 
'tsim-leon sunseeker' and then type 'go' in the Tsim prompt

To run the distributed version, run the 'sunseekercontroller'
executable then the 'sunseekerplant' one in two different terminals.
